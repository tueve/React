import React, { Component } from 'react';
import PropTypes from 'prop-types';

class NPMCompare extends Componnent {
  constructor(props) {
    super(props);
    this.state = {
      filter: '',
    };
  }

  render() {
    return (
      <div></div>
    );
  }
}

export default NPMCompare;
